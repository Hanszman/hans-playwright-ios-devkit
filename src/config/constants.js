const DEFAULT_URL = 'https://www.google.com';
const RUN_TIMEOUT_MS = 10 * 60 * 1000;
const NAVIGATION_TIMEOUT_MS = 120000;
const BLOCKED_ROUTE_PATTERN = '**/Home/BrowserNaoSuportado*';

module.exports = {
  DEFAULT_URL,
  RUN_TIMEOUT_MS,
  NAVIGATION_TIMEOUT_MS,
  BLOCKED_ROUTE_PATTERN,
};
